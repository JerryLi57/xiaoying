<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lolaage.crm.service.dao.mapper.CustomerFileMapper">

    <!-- 通用查询结果列 -->
    <sql id="baseColumnList">
        t.f_id AS id,
        t.f_customer_id AS customerId,
        t.f_file_id AS fileId,
        t.f_create_time AS createTime,
        t.f_update_time AS isdeleted,
        t.f_update_time AS updateTime
    </sql>

    <!-- 使用对象类型传参 -->
    <select id="listPageByDto" parameterType="com.lolaage.crm.service.dto.request.CustomerFileQueryDto" resultType="com.lolaage.crm.service.dto.response.CustomerFileListDto">
        SELECT <include refid="baseColumnList" />,
        f.f_name as fileName,
        f.f_ext as ext,
        f.f_type as type,
        f.f_file_des_id as fileDec
        FROM
        t_customer_file t
        LEFT JOIN t_file f on f.f_id = t.f_file_id
        WHERE
        t.f_isdeleted = 0
        <if test="dto != null">
            <!-- 这里写自己的条件拼装 -->
            <if test="dto.customerId !=null and dto.customerId > 0">
                and t.f_customer_id = #{dto.customerId}
            </if>
        </if>
        ORDER BY t.f_update_time DESC
    </select>


    <select id="listListByDto" parameterType="com.lolaage.crm.service.dto.request.CustomerFileQueryDto" resultType="com.lolaage.crm.service.dto.response.CustomerFileListDto">
        SELECT <include refid="baseColumnList" />,
        f.f_name as fileName,
        f.f_ext as ext,
        f.f_type as type,
        f.f_file_des_id as fileDec
        FROM
        t_customer_file t
        LEFT JOIN t_file f on f.f_id = t.f_file_id
        WHERE
        t.f_isdeleted = 0
        <if test="dto != null">
            <!-- 这里写自己的条件拼装 -->
            <if test="dto.customerId !=null and dto.customerId > 0">
                and t.f_customer_id = #{dto.customerId}
            </if>
        </if>
        ORDER BY t.f_update_time DESC
    </select>

</mapper>