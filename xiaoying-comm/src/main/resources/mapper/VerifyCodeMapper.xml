<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lolaage.crm.service.dao.mapper.VerifyCodeMapper">

    <!-- 通用查询结果列 -->
    <sql id="baseColumnList">
        t.f_id AS id,
        t.f_telephone AS telephone,
        t.f_verify_code AS verifyCode,
        t.f_type AS type,
        t.f_valid_time AS validTime,
        t.f_send_time AS sendTime,
        t.f_status AS status,
        t.f_use_time AS useTime
    </sql>

    <!-- 使用对象类型传参 -->
    <select id="listPageByDto" parameterType="com.lolaage.crm.service.dto.request.VerifyCodeQueryDto" resultType="com.lolaage.crm.service.dto.response.VerifyCodeListDto">
        SELECT <include refid="baseColumnList" /> FROM t_verify_code t where t.f_isdeleted=0
        <if test="dto != null">
            <!-- 这里写自己的条件拼装 -->

        </if>
        ORDER BY t.f_update_time DESC
    </select>

</mapper>