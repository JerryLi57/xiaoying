<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lolaage.crm.service.dao.mapper.ProjectLogMapper">

    <!-- 通用查询结果列 -->
    <sql id="baseColumnList">
        t.f_id AS id,
        t.f_project_id AS projectId,
        t.f_stage AS stage,
        t.f_isdeleted AS isdeleted,
        t.f_create_id AS createId,
        t.f_create_time AS createTime
    </sql>

    <!-- 使用对象类型传参 -->
    <select id="listPageByDto" parameterType="com.lolaage.crm.service.dto.request.ProjectLogQueryDto" resultType="com.lolaage.crm.service.dto.response.ProjectLogListDto">
        SELECT <include refid="baseColumnList" /> FROM t_project_log t where t.f_isdeleted=0
        <if test="dto != null">
            <!-- 这里写自己的条件拼装 -->

        </if>
    </select>

</mapper>